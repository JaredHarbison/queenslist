<% provide(:title, "home") %>
<h1><%= base_title %></h1>

  <% if session[:user_id] %> 
    <h1><%= @user.name %></h1>
    <h2>Email: <%= @user.email %></h2>
    <h2>Facebook UID: <%= @user.uid %></h2>
    <img src="<%= @user.image %>">
  <% else %>
    <%= link_to('Log in with Facebook!', '/auth/facebook') %></p> or </p>
  <% end %>
    <%= link_to "sign up now", signup_path, class: "btn btn-lg btn-primary" %>
